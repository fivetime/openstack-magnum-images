# kube-proxy in IPVS mode.
#
# Not needed with Cilium's kube-proxy replacement, and not needed by the
# default iptables proxier either. Present so a cluster that does select IPVS
# finds the modules already there; loading them costs nothing when unused.
ip_vs
ip_vs_rr
ip_vs_wrr
ip_vs_sh
