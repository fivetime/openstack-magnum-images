# Loading the bonding module creates bond0 immediately (max_bonds defaults
# to 1). A Cluster API node has a single virtio NIC, so that leaves a
# permanently down bond0 for every CNI and every node exporter to trip over.
# Keep the module available, create nothing.
options bonding max_bonds=0
